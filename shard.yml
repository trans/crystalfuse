name: crystalfuse
version: 0.3.0

authors:
  - Thomas Sawyer <transfire@gmail.com>
  - Stefan Merettig <stefan-merettig@nuriaproject.org>

crystal: 1.16+

license: MIT

# Build the C shim (c/libfusewrap.a) automatically on `shards install`, so a
# downstream user doesn't have to run `make` by hand. Needs gcc + fuse3 headers.
scripts:
  postinstall: make lib
