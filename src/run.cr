# probably temp file for testing
#require "./crystalfuse"

#mountpoint = ARGV[0]? || abort("Usage: run <mountpoint>")
#fs = Crystalfuse::FS.new
##fs.mount(mountpoint)
#fs.run!()

#require "./fusetest"
#mountpoint = ARGV[0]? || "./tmp/mnt"
#MyFS.new.mount(mountpoint)
